## Sistema de Reserva de Passagens

### Para rodar
   ```bash
        docker-compose up --build
   ```

### Corrigir erro de permição
 ```bash
        docker-compose down
        xhost +local:docker
        docker-compose up --build
   ```

